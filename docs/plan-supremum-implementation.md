# ARGUS Supremum Implementation Plan — June 12, 2026

> **Generated by:** plan agent (session `ses_142011bb2ffew5d1ZiW6vPQXzJ`)
> **Input:** `docs/supremum-roadmap.md` (568 lines, 20 items, 143h, EU AI Act Art. 12/19 enforcement Aug 2 2026)
> **Output:** 7-wave parallel task graph with per-item TDD scenarios, atomic commit strategy, risk register
> **Decisions adopted (D1/D2/D3):** C (hybrid module split) + A (keep inline dashboard) + YES (W0-1 spike)

## Discoveries (10 corrections to roadmap paths)

| # | Roadmap said | Actual | Plan impact |
|---|---|---|---|
| 1 | `argus-crypto/src/ledger.rs` | File is `chain.rs` (pure functions, no store) | Item 2.1/2.2 edit `chain.rs`; persistent store is net-new |
| 2 | `argus-llm/src/client.rs` | Files are `nim.rs` + `openai_compat.rs` + `mock.rs` | Item 3.1 wraps existing NimClient, no `client.rs` |
| 3 | `argus-verify/src/{routes,handler,cache}.rs` | Only `main.rs` + `lib.rs` (monolithic) | Items 2.2/6.2 create new modules or extend monolith (D1.C) |
| 4 | `apohara-argus-cli/src/commands/health.rs` | No `commands/`; Health inline at main.rs:62-214 | Item 2.4 extends inline |
| 5 | `argus-dashboard/{templates,static}/` | None; HTML is inline format! strings | Item 1.1 extracts to `templates.rs` module (D2.A) |
| 6 | `AuditEvent`/`DecisionArtifact` exist | **Neither exists** — roadmap-only spec | Item 2.1 creates both in core/src/types.rs |
| 7 | Integration tests `#[ignore]`'d | `tests/` is empty; 0 `#[ignore]`'d tests | TDD = new unit + integration tests |
| 8 | 7 commits on main | **10 commits** on main (4 doc-only) | Doesn't change plan |
| 9 | `syn`/`quote` deps present | **Not present** | Item 5.1 adds to workspace |
| 10 | Pre-existing LSP errors | **0 errors**, ~30 warnings (unused imports) | Doesn't block; auto-fixable |

## Wave Plan (7 waves, 10.5 day wall-time, 22-day budget)

```
Wave 0 (0.5d): Spikes [parallel]
Wave 1 (1d, 4h wall): 4.1, 6.1, 2.1, 3.1 [parallel, critical path to Aug 2]
Wave 2 (1d): 2.2 NDJSON export [depends on 2.1]
Wave 3 (0.5d, 3h wall): 8.1, 6.2, 2.4, 8.2 [parallel]
Wave 4 (2d, 15h wall): 1.1, 5.1 [parallel]
Wave 5 (1.5d, 14h wall): 6.3, 3.2, 1.2, 6.4, 7.1, 1.3 [mostly parallel]
Wave 6 (3d, defer): 4 EU AI Act L2, 5 MCP, 7.2 BYVK
Wave 7 (1d): Final verification (test+clippy+release+review)
```

## Per-Wave Detail (full specs in notepad /tmp/ulw-argus-*.md)

### Wave 1 — Phase 0 (4h wall time, 18h effort)

| Item | Category | Skills | Files | TDD | Commit |
|------|----------|--------|-------|-----|--------|
| 4.1 Model swap | quick | - | `argus-llm/src/{model_registry.rs(NEW),nim.rs}`, `apohara-argus-core/src/config.rs`, `argus-verify/src/lib.rs`, `.env.example` | 3+ tests: happy/edge/regression | `feat(llm): per-role model registry [Refs: 4.1]` |
| 6.1 Graceful shutdown | quick | - | `argus-verify/src/main.rs`, `argus-dashboard/src/main.rs` | 3+ tests: SIGTERM drains/SIGINT drain/regression | `feat(verify,dashboard): graceful shutdown [Refs: 6.1]` |
| 2.1 AuditEvent | unspecified-high | - | `apohara-argus-core/src/{types.rs,config.rs}`, `argus-llm/src/{audit.rs(NEW),lib.rs}`, `argus-verify/src/lib.rs` | 4+ tests: roundtrip/fingerprint/chain/schema | `feat(core,llm): AuditEvent Art.12 [Refs: 2.1]` |
| 3.1 Circuit breaker | ultrabrain | debugging | `argus-llm/src/{circuit_breaker.rs(NEW),retry.rs(NEW)}`, `openai_compat.rs`, workspace deps | 4+ tests: state cycle/open-no-call/concurrency | `feat(llm): circuit breaker [Refs: 3.1]` |

## Risk Register (top 5)

| # | Risk | Mitigation |
|---|------|------------|
| R1 | External crates (`llm-retry`, `a2a-rust`, `spiffe`) missing/broken on crates.io | W0-1 spike (de-risks 6 of 20 items) |
| R6 | Concurrent waves → merge conflicts in `verify/main.rs` and `llm/lib.rs` | D1.C: new logic in dedicated modules; sequential commits within wave |
| R4 | `AuditEvent` schema change forces re-export | `#[serde(default)]` on new fields |
| R3 | Aug 2 deadline slip (51d available, Phase 0 = 2-3d) | Buffer of 14+ days; 2.2 can slip 1d without breaking compliance |
| R5 | UI TDD breaks (1.1, 7.1) | `agent-browser` smoke test in CI; `visual-qa` skill |

## Definition of Done (1-month cycle)

- All 20 items complete OR explicitly deferred with rationale
- `cargo build --release` produces 4 binaries
- `argus-verify` EU AI Act Article 12 compliant (BLAKE3 chain + Ed25519 + NDJSON + 180d retention)
- `argus-dashboard` shows cohort view + handoff
- `argus-slop` runs deterministic pre-flight < 100ms
- ~30-35 atomic commits
- Working tree clean

# Third-Party Licenses

This file lists the licenses of all third-party crates compiled into the
`apohara-argus` release binaries. It is generated by `cargo about generate`
and regenerated in CI on every release. apohara-argus itself is licensed under
the `MIT` license (see the top-level `LICENSE` file).

{{#each licenses}}
## {{name}}

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```
{{text}}
```

{{/each}}
